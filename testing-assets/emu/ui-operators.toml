# ER-301 (stolmine) UI planning domain — TYPED OPERATOR LIBRARY.
#
# GENERATED by tools/extract_operators.py [stol:ui-planner-operators]; do NOT
# hand-edit. Regenerate + gate with `scripts/dev ui-operators`
# (STOL_UPDATE_UI_OPERATORS=1 accepts drift). Deterministic: sorted by id,
# byte-identical on an unchanged tree.
#
# Each [[operator]] = (pre fluents) --[gesture]--> (eff fluents). Fluents use
# the shared vocabulary in [fluents] (arity per name); `~` negates a
# precondition; {param} / {col_main} are gesture-template substitutions.
# `verified = static` means statically resolvable; `needs_crawl` means a live
# crawler must refine the gesture/precondition/effect (see each `note`).
# ─────────────────────────────────────────────────────────────────────────────

[meta]
schema = 1
generator = "tools/extract_operators.py"
total = 20
sources = ["manifest", "map", "ui_plan"]

[meta.by_source]
manifest = 4
map = 12
ui_plan = 4

[meta.by_verified]
needs_crawl = 6
static = 14

# Shared fluent vocabulary (name = arity). Must match the schema
# sibling (docs/UI_STATE_SCHEMA.md). `adjacent` is a static domain
# predicate used only in preconditions.
[fluents]
adjacent = 2
cell = 4
column_cursor = 1
context = 1
focused_class = 1
focused_unit = 1
linked = 2
modal = 1
slot_control = 2
unit_in_chain = 1

# Grid-sequencer column -> MAIN button (from xroot/Sequencer/
# GridView.lua kColNames). Resolves select_column's {col_main}.
[column_map]
cv1 = "MAIN1"
cv2 = "MAIN2"
g1L = "MAIN3"
g2L = "MAIN4"
stL = "MAIN5"
tr = "MAIN6"

[[operator]]
id = "collapse"
source = "manifest"
verified = "needs_crawl"
params = ["u"]
pre = ["context(home)", "focused_unit(u)"]
eff = []
gesture = ["press {u_header_main}", "frames 15"]
note = "Press u's header spot (the MAIN column whose control is Unit.Base.Header) to toggle to the collapsed view. CRAWLER-REFINED: the resulting slot_control(M*, ...) map is u's per-unit `views.collapsed` list (manifest units[u]); {u_header_main} is the live header column."

[[operator]]
id = "expand"
source = "manifest"
verified = "needs_crawl"
params = ["u"]
pre = ["context(home)", "focused_unit(u)"]
eff = []
gesture = ["press {u_header_main}", "frames 15"]
note = "Press u's header spot (the MAIN column whose control is Unit.Base.Header) to toggle to the expanded view. CRAWLER-REFINED: the resulting slot_control(M*, ...) map is u's per-unit `views.expanded` list (manifest units[u]); {u_header_main} is the live header column."

[[operator]]
id = "focus_unit"
source = "ui_plan"
verified = "needs_crawl"
params = ["u"]
pre = ["context(home)", "unit_in_chain(u)"]
eff = ["focused_unit(u)"]
gesture = ["turn 3", "frames 6"]
note = "CRAWLER-REFINED: encoder-scroll the chain SpottedStrip to u's section; detent count depends on u's live position in the chain."

[[operator]]
id = "insert"
source = "ui_plan"
verified = "needs_crawl"
params = ["u"]
pre = ["context(unit_picker_dense)"]
eff = ["unit_in_chain(u)", "focused_unit(u)", "context(home)"]
gesture = ["press MAIN1", "frames 30"]
note = "CRAWLER-REFINED: prefix with `turn N` to land the cursor on u's row (N = 3 raw detents/row) and pick MAIN1 (left) / MAIN4 (right) per u's cell in the live, channel-count-filtered, sorted picker list."

[[operator]]
id = "link"
source = "ui_plan"
verified = "static"
params = ["a", "b"]
pre = ["context(home)", "adjacent(a,b)", "~linked(a,b)"]
eff = ["linked(a,b)"]
gesture = ["down SELECT{a}", "frames 6", "down SELECT{b}", "frames 6", "up SELECT{b}", "up SELECT{a}", "frames 10"]
note = "Hold SELECT<a>, tap SELECT<b> (UserMode selectPressed). Both chains empty -> links with no dialog; context unchanged."

[[operator]]
id = "nav_admin_to_home"
source = "map"
verified = "static"
params = []
pre = ["context(admin)"]
eff = ["context(home)"]
gesture = ["storage up", "frames 10"]

[[operator]]
id = "nav_admin_to_sample_pool"
source = "map"
verified = "static"
params = []
pre = ["context(admin)"]
eff = ["context(sample_pool)"]
gesture = ["press MAIN2", "frames 12"]

[[operator]]
id = "nav_home_to_admin"
source = "map"
verified = "static"
params = []
pre = ["context(home)"]
eff = ["context(admin)"]
gesture = ["storage center", "frames 10"]

[[operator]]
id = "nav_home_to_hold"
source = "map"
verified = "static"
params = []
pre = ["context(home)"]
eff = ["context(hold)"]
gesture = ["mode up", "frames 10"]

[[operator]]
id = "nav_home_to_quicksave"
source = "map"
verified = "static"
params = []
pre = ["context(home)"]
eff = ["context(quicksave)"]
gesture = ["down SHIFT", "press MAIN1", "up SHIFT", "frames 12"]

[[operator]]
id = "nav_home_to_scope"
source = "map"
verified = "static"
params = []
pre = ["context(home)"]
eff = ["context(scope)"]
gesture = ["mode down", "frames 10"]

[[operator]]
id = "nav_home_to_unit_picker_dense"
source = "map"
verified = "static"
params = []
pre = ["context(home)"]
eff = ["context(unit_picker_dense)"]
gesture = ["press MAIN3", "frames 12"]

[[operator]]
id = "nav_quicksave_to_home"
source = "map"
verified = "static"
params = []
pre = ["context(quicksave)"]
eff = ["context(home)"]
gesture = ["press DIAL2", "frames 10"]

[[operator]]
id = "nav_sample_pool_to_admin"
source = "map"
verified = "static"
params = []
pre = ["context(sample_pool)"]
eff = ["context(admin)"]
gesture = ["press DIAL2", "frames 10"]

[[operator]]
id = "nav_scope_to_home"
source = "map"
verified = "static"
params = []
pre = ["context(scope)"]
eff = ["context(home)"]
gesture = ["mode center", "frames 10"]

[[operator]]
id = "nav_scope_to_sequencer"
source = "map"
verified = "static"
params = []
pre = ["context(scope)"]
eff = ["context(sequencer)"]
gesture = ["down SHIFT", "press ENTER", "up SHIFT", "frames 10"]

[[operator]]
id = "nav_unit_picker_dense_to_home"
source = "map"
verified = "static"
params = []
pre = ["context(unit_picker_dense)"]
eff = ["context(home)"]
gesture = ["press DIAL2", "frames 10"]

[[operator]]
id = "open_picker"
source = "ui_plan"
verified = "needs_crawl"
params = []
pre = ["context(home)", "slot_control(M3,EmptySection.EmptyControl)"]
eff = ["context(unit_picker_dense)"]
gesture = ["press MAIN3", "frames 12"]
note = "Nominal MAIN3 = the empty-insert column on a mono chain; after a stereo link it shifts to M4 (CRAWLER-REFINED: press whichever column holds EmptySection.EmptyControl). Justified by manifest EmptySection.EmptyControl enterReleased/subReleased -> chooser:open."

[[operator]]
id = "select_column"
source = "manifest"
verified = "static"
params = ["col"]
pre = ["context(sequencer)", "~column_cursor(col)"]
eff = ["column_cursor(col)"]
gesture = ["press {col_main}", "frames 6"]
note = "Resolve {col_main} via [column_map][col]. GridView:mainReleased(i) sets columnCursor=i-1 for a non-focused column; the same button on the focused column instead enters edit (hence ~column_cursor(col))."

[[operator]]
id = "set_cell"
source = "manifest"
verified = "needs_crawl"
params = ["slot", "col", "row", "v"]
pre = ["context(sequencer)", "column_cursor(col)"]
eff = ["cell(slot,col,row,v)"]
gesture = ["press ENTER", "turn 3", "frames 6"]
note = "Gesture: encoder-scroll focusHead to `row` (nav mode), ENTER -> editingL1, encoder-nudge to `v` (per-column step). CRAWLER-REFINED: row + nudge counts are dynamic. Direct-write equivalent used in tests: seq:setL1(slot,col,row,v) / seq:l1Value(...). Justified by manifest Sequencer.GridView (grid L1 editing)."
